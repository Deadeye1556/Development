EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# NOTE: No OpenAI / USDA key here on purpose. Those are server-only secrets,
# held by the Supabase Edge Functions (analyze-nutrition, analyze-photo) and
# set via `supabase secrets set` — never shipped in the Expo bundle (CTO-1).
# See supabase/functions/.env.example.
